# Heat Wave Prediction Project

This project predicts heat wave conditions using machine learning.

Note:
The dataset (CSV file) is not included due to size/privacy reasons.
To run this project:
1. Add your weather dataset as 'weather.csv'
2. Install required libraries
3. Run the notebook

Technologies Used:
- Python
- Pandas
- Scikit-learn
- Matplotlib